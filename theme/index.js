// Define the theme colors for the application
export const theme = {
    // Background color used across the application
    background: '#eab308',
    // Text color used across the application
    text: '#eab308'
}

// Define the styles for text and background using the theme colors
export const styles = {
    // Style for text elements, uses the text color from the theme
    text: { color: theme.text },
    // Style for background elements, uses the background color from the theme
    background: { backgroundColor: theme.background }
}