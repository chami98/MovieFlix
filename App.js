// Import the AppNavigation component from the navigation directory
import AppNavigation from './navigation/appNavigation';

// Define the App component
export default function App() {
  // Render the AppNavigation component
  return (
    <AppNavigation />
  );
}